<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:hardwareAccelerated="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.UnitCon"
        tools:targetApi="31">
        <activity
            android:name=".GuessGame"
            android:exported="false"
            android:label="Number Guessing Game"
            android:parentActivityName=".MainActivity"/>
        <activity
            android:name=".Kilometer"
            android:exported="false"
            android:label="Convert Kilometer To Miles"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".Kilogram"
            android:exported="false"
            android:label="Convert Kilograms To Pounds"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".Centimeter"
            android:exported="false"
            android:label="Convert Centimeter To Meter"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>